import { StyledMain } from '../styles';
const Contacts = (props) => {
    return (
        <StyledMain>
            <h1>Show Contact Page!</h1>
        </StyledMain>
    );
};

export default Contacts;