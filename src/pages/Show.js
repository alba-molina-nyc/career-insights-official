import { StyledMain } from '../styles';
const Show = (props) => {
    return (
        <StyledMain>
            <h1>Career insights Show Page!</h1>
        </StyledMain>
    );
};

export default Show;